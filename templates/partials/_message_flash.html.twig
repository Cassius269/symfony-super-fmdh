{# Lire et afficher tous les messages flash #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash-{{ label }} text-white text-center bg-primary p-3">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}